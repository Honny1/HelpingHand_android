<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mnecas.helpinghand">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--android:theme="@style/AppTheme"-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main.Register" />
        <activity
            android:name=".Main.MainActivity"
            android:label= "Helping Hand"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".popups.ConfigPopup"
            android:theme="@style/AppTheme.ConfigWindowTheme" />
        <activity
            android:name=".popups.AddConfigPopup"
            android:theme="@style/AppTheme.ConfigWindowTheme" />
    </application>

</manifest>